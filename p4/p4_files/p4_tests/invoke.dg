int f(int arg1,bool arg2){
}
void g(){
	int a;
	f(a, true);
	g();
	bool aRandomBool;
	if(g <= (1+a or aRandomBool))
	{
		output true;
		return true;
	}
	else
	{
		int n;
		for(int i; i<n; i++)
		{
			i = n+i;
			aRandomBool = !aRandomBool;
			f(a, true);

		}
		return false;
		while(aRandomBool)
        {
            g();
			f(a, true);
        }
	}
	
}
